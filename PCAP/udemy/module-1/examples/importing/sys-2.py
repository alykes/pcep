# This now means that you don't need to use the namespace to call the function
# We see that we can import the direct entities from the module that we are interested in
from sys import exit

# The valid way to call is now exit() and sys.exit() is no longer required
exit()